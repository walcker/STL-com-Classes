#include <iostream>
#include <string>
#include <vector>
#include "classe.h"

int Classe::area() {
    return largura * altura;
}

int Classe::perimetro() {
    return (2 * largura + 2 * altura);
}

int Classe::Vetor(){
  for(int i = 0; i < 5; i++){
  return (vetor[i]*2);
 }
 return 0;
}


